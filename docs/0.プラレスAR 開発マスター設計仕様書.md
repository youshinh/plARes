# プラレスAR 開発マスター設計仕様書

## 1. プロジェクト概要：「プラレスAR」とは

本プロジェクトは、現実空間（リビングの床や机の上、自作の木工作品など）を舞台に、自分だけのオリジナルAIロボット（プラレスラー）を召喚し、声とジェスチャーで指示を出して戦わせる「次世代WebAR×AIマルチエージェント・バトルゲーム」です。
単なる強い機体の殴り合いではなく、「競技性」と「モノづくり（DIY）」の喜びを融合させています。さらに、専用アプリのインストールを不要とし、URL共有のみで観客（オーディエンス）がブラウザ経由で乱入できるWebARベースの高度なエンタメシステムとして構築されます。

## 2. システム全体のハイブリッド・アーキテクチャ

アクションゲームに必須の「ゼロ遅延（競技性）」と、AIエージェントの「高度な推論・自律性」を両立させるため、システム全体を**「フロントエンド（体・感覚）」と「バックエンド（脳・戦略）」に完全に分離した非同期ハイブリッド設計**を採用しています。

システムは以下の4つのモジュールで構成されます。

- **モジュールA：フロントエンド（体・感覚）**
  - **技術スタック**: React, Three.js, WebXR Device API
  - **役割**: アプリ不要のWebAR環境を構築します。スマホカメラから現実空間の床や段差をスキャンし、Hit Test APIとDepth Sensing APIを用いてリアルタイムに3Dメッシュ化を行い、機体が移動可能なNavMesh（ナビゲーションメッシュ）を動的に生成します。また、通信ラグを嫌う移動や防御の基本操作は、ブラウザ標準の Web Speech API を用いてローカルで遅延ゼロで処理します。
- **モジュールB：バックエンドAI（脳・戦略）**
  - **技術スタック**: Python, Agent Development Kit (ADK) / Antigravity, Gemini 3.1 Pro (Multimodal Live API)
  - **役割**: スマホからの負荷処理を受け持ちます。セッションごとにスレッドセーフな非同期バッファである `LiveRequestQueue` を生成し、`asyncio.gather()`を用いたノンブロッキングな並行タスク制御により、映像（1〜2fps）と音声波形を継続的にストリーミングします。これにより、ユーザーからの割り込み（Interruption）をシームレスに処理し、滑舌判定や戦術指示（Function Calling）、実況生成などの推論を各エージェントとして常駐させます。
- **モジュールC：リアルタイム通信網（神経）**
  - **技術スタック**: WebRTC (Data Channel / Media Stream), WebSocket
  - **役割**: プロトコルを厳密に分離します。機体の座標や基本アクションの同期には、ラグを10〜30msに抑えるUDPベースの WebRTC Data Channel を用いたP2P通信を採用します。一方、AI向けの重いマルチメディア（音声・映像）転送には WebRTC Audio/Video Stream を、言語非依存のイベント同期（例：ダメージ通知）には WebSocket を利用します。
- **モジュールD：データ・記憶基盤（状態管理）**
  - **技術スタック**: Google Cloud Firestore, Firebase Authentication
  - **役割**: 試合中の高頻度なRead/WriteによるAPIコスト破綻と遅延を防ぐため、試合中のリアルタイムな状態管理（State Management）はADKのローカルメモリやセッション内でのみ保持します。試合終了時にのみ、AIにイベントを要約（Summarize）させ、Firestoreの長期記憶（aiMemorySummary）や試合履歴（matchLogs）としてUpdateします。

## 3. コア機能の実装要件と設計図

### 3.1. 3層優先度制御ステートマシン（FSM）

通信遅延によるカクつきを防ぎ、自然な自律挙動を実現するため、フロントエンドのレンダリングループ（`requestAnimationFrame`）内に、3層の優先度（Priority）制御を実装します。

1.  **Priority 1（高）ローカル音声の緊急割り込み**: Web Speech APIを用いて「避けろ！」などのプレイヤーの音声をローカルで検知し、機体の移動ベクトルに強力な物理インパルスを即座に加え、ラグゼロで回避させます（クラウド通信なし）。
2.  **Priority 2（中）AIからのマクロ戦術割り込み**: Gemini APIから非同期で送られてくるJSON形式の Function Calling（例：`{"action": "take_cover", "target_object": "obstacle_A"}`）を受信し、ステートと移動目標座標を強制的に上書きします。
3.  **Priority 3（低）ローカル基本ループ**: 平常時は、生成されたNavMesh上を経路探索（Pathfinding）アルゴリズムで毎フレーム計算し、自律的に移動させます。
    （詳細: `3.3層優先度制御ステートマシン設計書`）

### 3.2. モーション先行型のレイテンシ隠蔽と滑舌判定

AIの推論やAPI通信に伴うラグ（1〜2秒）をアクションゲームのUX上で完全に隠蔽する設計です。

- **レイテンシ・ハイディング**: プレイヤーが必殺技の発声トリガーを引いた瞬間、フロントエンドで即座に約3秒の「チャージアニメーション」を開始します。この描画と完全に並行して、バックエンドへ詠唱音声のストリーミングを行います。
- **Native Audio解析**: 送信された音声は、STT（テキスト化）をバイパスし、生の音声波形（16kHz PCM）のままGemini 3.1 ProのLive APIへ入力されます。滑舌判定エージェントが「正確さ」「スピード」に加え、波形から読み取れる「気迫（声の大きさや震え）」の3軸でスコアを算出し、アニメーション終了までにクリティカル判定のJSONをフロントエンドに返します。
  （詳細: `5.滑舌判定・レイテンシ隠蔽システム設計書`）

### 3.3. Geminiネイティブ機能のパラダイムシフト

LLMを単なるテキストジェネレータではなく、自律エージェントとしてフル活用します。

- **Context Caching**: 膨大な「過去の戦歴」や「物理ルール」をゲーム起動時に一度だけキャッシュし、入力トークンコストを劇的に削減しつつ、初動のトークン生成（TTFT）を爆速化します。
- **Managed MCP servers for Firestore**: 開発者が手動でデータベース検索APIを組むのではなく、AI自身にFirestoreへのクエリ実行権限（ツール）を付与します。これにより、AIが対話の文脈や現在の風景（Vision検知）から自律的に過去の記憶（aiMemorySummary）を検索し、「1ヶ月前のあの障害物の時、焦ってたよな」といった成長フィードバック（Proactive Audio）を自発的に生成します。
  （詳細: `6.プラレスAR：データ構造とMemory Bank統合設計書.md` および `7.プラレスAR：AI人格・ステータス進化設計書`）

### 3.4. マルチモーダル生成AIによる拡張エンタメ機能

観客の介入や報酬システムに、画像・動画・音楽生成APIを連携させます。

- **現実融合クラフト（Nano Banana Pro）**: 観客がスマホで撮影した現実のアイテム（例：夕食の春巻き）の写真と「巨大隕石」といったテキストプロンプトを受信し、Gemini 2.5 Flash Image（8-Image Mix機能）を用いて、1〜2秒で「現実の質感を残した3Dテクスチャやオブジェクト」を動的生成し、アリーナへ投下します。
- **マイルストーン動画・凱歌生成（Veo 3.1 / Lyria API）**: ダイナミック・ゲーティングの思想に基づき、試合終了時や5試合ごとのマイルストーンに限定して非同期バッチ処理を起動します。試合のテキストログとスクリーンショットをマルチモーダル入力とし、シネマティックなハイライト映像や、試合展開に応じたEDM等の勝利の凱歌を自動生成し、SNSへのシェア（バイラル拡散）を促進します。
  （詳細: `9.マルチモーダル生成AI・現実融合クラフト設計書`）

### 3.5. 言語非依存通信とServer-Driven UI

多言語対応は、静的な辞書ファイルではなく、ハイブリッド構成で実装します。

- 通信ペイロードには `{"event": "critical_hit", "user": "player1"}` といった言語非依存の軽量なJSONデータのみを使用し、戦術パネルや実況字幕はバックエンドAIが生成したJSONを受け取ってReactのStateにバインドし、動的に描画（Server-Driven UI）します。
- 試合終了後には、勝者のAIが相手の言語設定を読み取り、相手の母国語に自動翻訳して煽りや賞賛の「勝利者インタビュー」をGemini TTSでストリーミング再生するエモーショナル翻訳機能が稼働します。
  （詳細: `10.次世代プラレス：多言語対応・動的UI設計書`）

### 3.6. 1試合の標準ライフサイクル（確定仕様）

本プロジェクトの試合進行は、以下の時系列を正仕様として実装します。

1. **Pre-Match（ルーム生成・空間同期）**
   - ホストがルームを作成し、対戦相手はURL/QRから参加。
   - WebRTCシグナリングを確立後、ローカル対戦はCloud Anchors同期、遠隔対戦はホスト地形JSON重畳でアリーナを共有。
   - バックエンドはセッション単位で `LiveRequestQueue` を初期化し、Context Cachingをロード。
2. **Match Start（開幕）**
   - 両者の初期座標をWebRTC Data Channelで同期し、FSMを `State.HOVERING` で開始。
3. **Normal Loop（通常戦闘）**
   - 毎フレーム、Priority 1 -> 2 -> 3の順で評価。
   - Priority 3はNavMesh経路探索によるローカル自律移動。
4. **Special Move（必殺技）**
   - トリガー時に `State.CASTING_SPECIAL` へ遷移し、3秒チャージ演出を即時再生。
   - 演出と並行してNative Audioをバックエンドへ送信し、判定JSON（Critical/Miss）を受信。
5. **Audience Intervention（観客介入）**
   - 観客の画像+テキスト入力を受け、必要時のみ生成AIを起動してアイテム/テクスチャを投下。
6. **Match End（勝敗確定）**
   - HP 0到達で終了イベントをブロードキャスト。
   - 勝利者インタビューを敗者の言語でTTS生成。
7. **Result Commit（記録と報酬）**
   - 試合中のインメモリ状態を要約し、`matchLogs` と `aiMemorySummary` を更新。
   - 勝利時Lyria、5試合ごとVeoの非同期バッチを起動。

### 3.7. 競技運用規約（競合解決・フォールバック）

仕様の曖昧さを排除するため、以下を全モジュール共通の運用規約とします。

- **最終決定順位（高 -> 低）**
  1. Priority 1: ローカル緊急回避（Web Speech/VAD）
  2. 対戦相手からの確定同期イベント（被弾・補正イベント）
  3. Priority 2: AI戦術割り込み（Function Calling）
  4. Priority 3: ローカル基本ループ
- **ロールバック窓**
  - P2P遅延を吸収するため、被弾判定と回避入力の競合は `50ms` 以内なら回避優先で再計算。
- **AI未応答フォールバック**
  - 必殺技判定が演出終了までに未着なら `Miss` 扱いで進行を継続（試合停止禁止）。
- **切断時の取り扱い**
  - ハートビート欠落で切断疑いに移行し、一時停止。
  - 猶予時間内の再接続で状態復元、超過時は切断側敗北（TKO）。
- **不正対策**
  - 移動ベクトル・速度上限・座標飛びをサーバー側で検証し、異常値は破棄して補正イベントを配信。

### 3.8. 実装パラメータ初期値（調整可能）

初期リリース時の規定値を以下とし、βテストで微調整します。

| パラメータ           | 初期値        | 許容レンジ / 備考              |
| :------------------- | :------------ | :----------------------------- |
| P2P座標同期レート    | `30Hz`        | `30-60Hz`                      |
| 目標P2P遅延          | `10-30ms`     | 超過時は補間強化               |
| 必殺技チャージ演出   | `3.0s`        | `2.5-3.5s`                     |
| 必殺技判定待機期限   | `演出終了時`  | 未着ならMissフォールバック     |
| AI戦術ポーリング間隔 | `3s`          | `2-5s`                         |
| 映像ストリーム       | `1fps`        | `1-2fps`（ゲーティング時のみ） |
| 観客介入総回数       | `3回/試合`    | 1回ごとに全体クールダウン      |
| 観客介入クールダウン | `30s`         | 運用で調整                     |
| 介入アイテム効果上限 | `最大HPの20%` | 単発逆転の抑止                 |
| 切断判定             | `3秒`         | ハートビート欠落ベース         |
| 再接続猶予           | `15秒`        | 超過でTKO                      |

### 3.9. 修行モード・散歩モード（育成ループ）

対戦以外の成長導線として、以下の2モードを正式仕様に含めます。

- **修行モード（シンクロ特訓）**
  - ローカル回避ドリル -> 早口詠唱ドリルの2段階で進行。
  - 評価軸は `accuracy / speed / passion`。
  - 結果は `trainingLogs` と `aiMemorySummary` に統合し、`syncRate` を増減。
- **散歩モード（探索・記憶強化）**
  - WebXR NavMesh上で安全制約付きの自律移動を実行。
  - VisionトリガーでProactive Audioを発火し、過去記憶と現在風景を連動した発話を生成。
  - 収集素材は `inventory` と `walkLogs` に保存し、次回対戦の演出・生成素材に利用。

この2モードで育成した記憶・ステータスは、必ず次回対戦セッションへ持ち越します。

---

## 4. プラレスAR 開発マスター設計図書一覧

システム全体を詳細化した各設計書は以下の通りです。

| 設計図書名                                           | 対象モジュール・レイヤー       | 役割と記載すべき主要技術仕様（コード実装前提）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| :--------------------------------------------------- | :----------------------------- | :---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **1. マスターアーキテクチャ設計指示書 (AGENT.md)**   | システム全体                   | プロジェクトの全体像、フロントエンドとバックエンドの非同期ハイブリッド設計の定義を行います。Agent Development Kit (ADK) などのマルチエージェント基盤の選定、GCPサービス一覧、およびAIエージェントへの段階的実装指示（Developer Knowledge MCP serverの常時接続など）を明記します。                                                                                                                                                                                                                                                                                                         |
| **2. フロントエンド・空間認識＆自律移動設計書**      | モジュールA(体・感覚)          | アプリ不要のWebAR環境を構築するため、ReactとThree.js、およびWebXR Device API（Hit Test / Depth Sensing）を用いた現実空間の3Dメッシュ化とNavMesh（移動可能エリア）の動的生成パイプラインを定義します。また、Web Speech APIを利用した遅延ゼロのローカル音声入力処理についても設計します。                                                                                                                                                                                                                                                                                                   |
| **3. 3層優先度制御ステートマシン（FSM）設計書**      | モジュールA(体・感覚)          | フロントエンドのレンダリングループ（requestAnimationFrame等）内で実行される機体の自律行動ロジックを定義します。Priority 3（NavMesh経路探索による基本移動）、Priority 2（Geminiからの非同期Function Callingによる戦術割り込み）、Priority 1（ローカル音声検知による緊急の物理インパルス）の順序と状態遷移ロジックを記述します。                                                                                                                                                                                                                                                            |
| **4. バックエンド・双方向ストリーミング制御設計書**  | モジュールB(脳・戦略)          | Gemini 3.1 ProのMultimodal Live API (Bidi-streaming) とADKの統合設計です。セッションごとの LiveRequestQueue（スレッドセーフなFIFOバッファ）の生成、映像（1〜2fps）と音声波形の継続的な流し込み、Pythonの asyncio.gather() を用いたノンブロッキングな並行制御（割り込み・Interruption処理）を定義します。                                                                                                                                                                                                                                                                                  |
| **5. 滑舌判定・レイテンシ隠蔽システム設計書**        | モジュールB & UI(戦闘ロジック) | 必殺技発動時のAPI推論ラグ（1〜2秒）を完全に隠蔽するため、フロントエンドでの「3秒間のチャージアニメーション先行再生」とバックエンドへの非同期通信を連動させるアーキテクチャを定義します。また、Native Audioを用いた音声波形による3軸評価（正確さ、スピード、気迫の解釈）の採点ロジックを設計します。                                                                                                                                                                                                                                                                                       |
| **6. データ構造・Memory Bank統合設計書**             | モジュールD(データ・記憶基盤)  | Firestoreのサブコレクション階層（users/{userId}/robots/{robotId}等）と、APIコスト破綻を防ぐ「試合中のインメモリ状態管理（State Management）」の分離を定義します。また、Context Cachingによる初動トークン生成（TTFT）の高速化と、 Managed MCP servers for Firestore を用いたAIの自律的データベース検索の権限付与を設計します。                                                                                                                                                                                                                                                             |
| **7. AIキャラクター・パラメーター＆人格進化設計書**  | AI人格・ステータス             | ハードウェア（材質）、ソフトウェア（トーク力など）、ネットワーク（シンクロ率）の3層パラメーターを定義します。イベントログの抽出と長期記憶（aiMemorySummary）の更新、Visionトリガーによる自発的発話（Proactive Audio）、およびTone Controlによる性格の動的進化（ダイナミック・ペルソナ・シフト）のシステムプロンプト実装を定めます。                                                                                                                                                                                                                                                       |
| **8. リアルタイム通信網・マルチプレイ同期設計書**    | モジュールC(神経)              | 用途に応じたプロトコルの使い分けを定義します。機体座標やアクション同期には超低遅延の WebRTC Data Channel (UDP/P2P) を、AIとのストリーミングには WebRTC Audio/Video Stream を、JSONのイベント通知には WebSocket を採用します。また、Cloud Anchorsや仮想アリーナ重畳方式による空間同期マッチングのフローを定義します。                                                                                                                                                                                                                                                                      |
| **9. マルチモーダル生成AI・拡張パイプライン設計書**  | エンタメ＆拡張機能             | 観客のスマホで撮影した現実のアイテム写真とテキストから、Nano Banana Pro (Gemini 2.5 Flash Image) の「8-Image Mix」を用いて1〜2秒で3Dテクスチャを動的生成・マッピングする「現実融合クラフト」の実装を定義します。また、Veo 3.1とLyria APIを用いたマイルストーン動画や凱歌の非同期バッチ生成（イベント駆動型のダイナミック・ゲーティング）を設計します。                                                                                                                                                                                                                                    |
| **10. 多言語対応・動的UI（Server-Driven UI）設計書** | UI / フロントエンド            | 戦術パネルや実況字幕などのUIをハードコードせず、バックエンドAIが生成する言語非依存のJSONペイロードを受け取り、ReactのStateにバインドしてコンポーネントを動的に描画する機構を定義します。また、トランスレーター＆実況エージェントによる、プレイヤーの母国語へのリアルタイムTTS出力と感情（Sentiment）分析の統合を設計します。                                                                                                                                                                                                                                                              |
| **11. ゲームシステム補完設計仕様書**（⭐必読）       | 全モジュール横断               | 既存設計書（Doc.1〜10）において未定義だったゲームシステムの実装定数・ルールを補完します。**マテリアル三すくみ補正倍率**（Wood/Metal/Resin の1.3×/1.0×/0.8×）、**EXゲージ蓄積ロジック**（攻撃ヒット+8・被弾+12・時間+1/秒、MAX=100）、**HP計算式**（100+vit×2）、**ダメージ計算式**、**ヒートシステム発動条件**、**マテリアル取得タイミング**（Firebaseシグナリング時）、**開発フェーズロードマップ**（MVP=ローカルフレンド対戦優先）、**確定通信プロトコル分離ルール**、**観客接続仕様**（同一WSルーム・数名規模）を定義します。                                                          |
| **12. ゲーム機能拡張仕様書**（⭐必読）               | 全モジュール横断               | NotebookLM照会で確認された拡張機能の詳細仕様をまとめます。**修行モード**（`totalTrainingSessions`）・**散歩モード**（`totalWalkSessions`）とFirestoreスキーマ、**ロボット外見の動的進化**（ボーン制御＋プロシージャルテクスチャ＋装備アタッチ）、**観客応援バフシステム**（talkSkillによる確率制御・ペルソナシフト・アイテム拒絶）、**勝利者インタビュー**（エモーショナル翻訳・Gemini TTS・Lyria BGM非同期生成）、**DIYアリーナ**（30cmグリッド規格・障害物HP・マテリアルオーブ・仮想アリーナ重畳）、**プロアクティブ音声**（Vision連動トリガー・MCP記憶検索・15文字制約）を定義します。 |

> ⚠️ **実装前に必ず Doc.11・Doc.12（補完・拡張設計書）を確認すること。** 既存 Doc.1〜10 と内容が矛盾する場合は Doc.11/12 を優先する。
