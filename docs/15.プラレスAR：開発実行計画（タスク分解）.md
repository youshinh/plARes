# 15.プラレスAR：開発実行計画（タスク分解）

最終更新日: 2026-02-28
対象期間: 2026-03-02 〜 2026-04-10（6週間）

## 1. 目的

`Engineering_Living_WebAR_AI.pdf` と現状実装の差分を埋め、
「デモ品質」から「継続運用できる開発品質」へ移行する。

- 重点1: コア体験（WebXR/FSM/低遅延同期）の完成度向上
- 重点2: モック実装の本実装化（生成AI・パイプライン）
- 重点3: 開発運用ルール（`PROGRESS_SYNC.md`、テスト、CI）の定着

## 2. 実行ルール（全タスク共通）

- ディレクトリ責務を厳守する
  - Agent 1: `/frontend`
  - Agent 2: `/backend/ai_core`
  - Agent 3: `/backend/infrastructure` と `/shared`
- Contract-First: 先に `/shared/types` の型を更新してから実装
- 進捗同期: `PROGRESS_SYNC.md` を必ず更新して着手/完了を記録
- 完了条件（DoD）を満たすまで「完了」にしない

## 3. フェーズ計画

## Phase 0（W1前半）: 開発基盤の是正

### T0-1: 進捗同期ファイル運用の開始（最優先）
- 担当: Agent 3
- 対象: `/PROGRESS_SYNC.md`
- 実施内容:
  1. `PROGRESS_SYNC.md` を新規作成
  2. テンプレートを定義（Owner/Task/Status/Start/ETA/Blocker）
  3. 既存タスクを初期登録
- DoD:
  - `PROGRESS_SYNC.md` がリポジトリ直下に存在する
  - 3エージェント分の現在タスクが1件以上記録されている

### T0-2: バックエンドテストハングの修正
- 担当: Agent 2
- 対象: `/backend/ai_core/streaming/bidi_session.py`, `/backend/tests/test_streaming.py`
- 実施内容:
  1. `test_handle_client_connection` がタイムアウトする原因を特定
  2. `listen_task` / `run_task` の終了条件を明示
  3. 単体テストを安定通過させる
- DoD:
  - `PYTHONPATH=backend backend/venv/bin/python -m pytest backend/tests -q` がハングせず完了
  - `test_handle_client_connection` が安定してパス

### T0-3: 最低限CIの固定化
- 担当: Agent 3
- 対象: `.github/workflows/*`
- 実施内容:
  1. Frontend build
  2. Backend py_compile
  3. Backend unit tests
- DoD:
  - PRで自動実行される
  - 失敗時に原因ログが追える

## Phase 1（W1後半〜W2）: Frontend体験完成度の引き上げ

### T1-1: Depth Occlusion 配線修正
- 担当: Agent 1
- 対象: `/frontend/src/App.tsx`, `/frontend/src/components/RemoteRobotCharacter.tsx`
- 実施内容:
  1. `useWebXRScanner()` の `depthTexture`, `depthRawToMeters` を `RemoteRobotCharacter` へprops連携
  2. `MainScene` での受け渡し経路を実装
- DoD:
  - AR対応端末で遮蔽物の裏にロボットが隠れる
  - 非対応端末では正常フォールバック

### T1-2: シェーダーパッチの実利用化
- 担当: Agent 1
- 対象: `/frontend/src/components/RemoteRobotCharacter.tsx`, `/frontend/src/utils/depthOcclusion.ts`
- 実施内容:
  1. `patchDepthOcclusionMaterial` をmesh materialに適用
  2. `occlusionMaterialsRef` へ管理対象材質を登録
  3. material差し替え時のdispose漏れを解消
- DoD:
  - `updateDepthOcclusionUniforms` が実材質に反映される
  - メモリリークが発生しない

### T1-3: FSM行動のデバッグ可視化
- 担当: Agent 1
- 対象: `/frontend/src/components/ui/AnimationDebugPanel.tsx` ほか
- 実施内容:
  1. 現在FSM状態/Priority出所（P1/P2/P3）をUI表示
  2. 状態遷移ログを短期バッファで確認可能にする
- DoD:
  - 緊急回避がAI命令を上書きする様子を画面で確認できる

### T1-4: AR平面未検出時のガイド強化
- 担当: Agent 1
- 対象: `/frontend/src/hooks/useWebXRScanner.ts`, `/frontend/src/components/ui/DynamicSubtitle.tsx`
- 実施内容:
  1. 平面検出/未検出/再試行中のUI文言を状態別に表示
  2. 位置合わせ失敗理由の提示
- DoD:
  - 初回ユーザーが操作手順で迷わない

## Phase 2（W3）: AI Core安定化

### T2-1: `/ws/live` とブラウザLive接続方式の統一方針決定
- 担当: Agent 2
- 対象: `/backend/ai_core/main.py`, `/backend/ai_core/streaming/bidi_session.py`, `/frontend/src/services/GeminiLiveService.ts`
- 実施内容:
  1. 「Browser直結」か「Backend中継」かを決定
  2. 未採用側を縮退または検証用途に明示
  3. README/運用手順を更新
- DoD:
  - 本番経路が1本に定義され、開発者が迷わない

### T2-2: 音声判定ロジックの実運用チューニング
- 担当: Agent 2
- 対象: `/backend/ai_core/main.py`
- 実施内容:
  1. `critical_threshold` と `sync_bonus` の調整
  2. 誤判定ログ（critical/miss）を追跡できる計測追加
  3. 少なくとも3パターンの音声ケースで手動検証
- DoD:
  - 体感に反する誤判定率が低下
  - ログで原因分析可能

### T2-3: キャラクター生成APIの失敗時動線改善
- 担当: Agent 2
- 対象: `/backend/ai_core/character_generator.py`, `/frontend/src/hooks/useCharacterSetup.ts`
- 実施内容:
  1. 失敗理由コードを構造化
  2. フロントに再試行可能な文言を返却
- DoD:
  - 生成失敗時でもユーザーが復帰しやすい

## Phase 3（W4〜W5）: Infrastructure本実装化

### T3-1: マルチモーダル生成パイプラインのモック脱却
- 担当: Agent 3
- 対象: `/backend/infrastructure/multimodal_pipeline.py`
- 実施内容:
  1. `RealityFusionCrafter.generate_fused_item` の実API接続
  2. `MilestoneVideoGenerator.publish_message` の実Pub/Sub化
  3. API失敗時リトライ/サーキットブレーカ追加
- DoD:
  - モックURLではなく実成果物URLが返る
  - 非同期ジョブが失敗時に再試行される

### T3-2: MCP Tool呼び出しの実行系整備
- 担当: Agent 3
- 対象: `/backend/infrastructure/mcp_server.py`, `/backend/ai_core/main.py`
- 実施内容:
  1. Firestore検索ツール定義だけでなく実行レスポンス連携を確認
  2. Interactionフローでツール実行結果がAI応答へ反映されることを検証
- DoD:
  - 過去対戦ログ参照を使った応答が再現できる

### T3-3: Context Cache運用改善
- 担当: Agent 3
- 対象: `/backend/infrastructure/vertex_cache.py`, `/backend/ai_core/main.py`
- 実施内容:
  1. ユーザー別cache TTL/再生成条件を定義
  2. キャッシュヒット率・作成失敗率をログ計測
- DoD:
  - 再接続時TTFT改善が確認できる

### T3-4: `backend/infrastructure` 未統合実装の整理
- 担当: Agent 3
- 対象: `/backend/infrastructure/bidi_stream.py`, `/backend/infrastructure/state_manager.py`
- 実施内容:
  1. `main.py` と重複している責務を統合または廃止
  2. 現在使用されない実装には「非採用」注記を追加
- DoD:
  - どの実装が本番利用なのか一意になる

## Phase 4（W6）: 結合検証とリリース準備

### T4-1: E2Eシナリオの拡張
- 担当: Agent 1+2+3
- 対象: `/scripts/e2e_live_smoke.sh`
- 実施内容:
  1. 現行「Token発行→Live接続」に加え、対戦イベント確認を追加
  2. `critical_hit` / `match_end` / `winner_interview` の最短導線を検証
- DoD:
  - 1コマンドで主要導線を自動検証できる

### T4-2: 性能・コスト基準チェック
- 担当: Agent 3
- 指標:
  - WebRTC座標同期: 10〜30ms目標
  - 音声判定レスポンス: 3秒詠唱内で収束
  - Firestore書き込み: 試合中は高頻度書き込みしない
- DoD:
  - 実測レポートが1ファイルで提出される

### T4-3: リリースチェックリスト作成
- 担当: Agent 3
- 対象: `/docs/release_checklist.md`
- 実施内容:
  1. 環境変数
  2. APIキー/権限
  3. 監視項目
  4. ロールバック手順
- DoD:
  - 新規メンバーでも手順通りにデプロイ可能

## 4. 優先バックログ（直近10営業日）

1. T0-1 `PROGRESS_SYNC.md` 作成と運用開始
2. T0-2 テストハング修正
3. T1-1 Depth情報の配線
4. T1-2 Depth shader実適用
5. T2-1 Live経路の一本化
6. T3-1 モック生成パイプラインの本実装着手

## 5. タスク管理テンプレート（`PROGRESS_SYNC.md` 用）

```md
| Date | Owner | Task ID | Directory | Status | Next Action | Blocker |
|------|-------|---------|-----------|--------|-------------|---------|
| 2026-03-02 | Agent1 | T1-1 | /frontend | IN_PROGRESS | App.tsxでdepth props連携 | なし |
```

ステータス定義:
- `TODO`: 未着手
- `IN_PROGRESS`: 実装中
- `BLOCKED`: 依存待ち
- `DONE`: DoD達成済み

## 6. 完了判定

以下を満たした時点で本計画を完了とする。

- P0〜P4の全タスクが `DONE`
- モック扱いだった主要機能（Fusion/Video/Music）が本実装済み
- E2E + 単体テストがCIで安定通過
- `PROGRESS_SYNC.md` が継続運用されている
